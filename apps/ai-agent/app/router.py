"""
Grabit AI Agent — API router.

Wires the agent endpoints (diagnose / message / explain) defined in
app/agents/*.py onto the FastAPI app in main.py. Chunk 1: empty placeholder
so the module structure exists.
"""
from fastapi import APIRouter

router = APIRouter()
