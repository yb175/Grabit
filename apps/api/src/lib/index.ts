// Grabit API — internal library helpers.
//
// Eventually: webhook signature verification, WhatsApp provider client
// (message sending), AI-agent HTTP client (calls the Python FastAPI service),
// and auth middleware. Chunk 1: intentionally empty placeholder so the
// directory exists in the skeleton.
export {}
